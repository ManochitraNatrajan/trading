services:
  - type: web
    name: prathik-backend
    env: node
    rootDir: prathik-backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 4000
      - key: NODE_ENV
        value: production
